// This is a Single Page Application (SPA)
// Disable server-side rendering and enable client-side routing
export const ssr = false;

// Only prerender the static pages, not dynamic routes
export const prerender = false;